## 14:30 - digiliveの構成整理とmacOSセットアップ追加
- **実施内容**: ルート直下に混在していた静的WebとNFCツールを `apps/` 配下へ分離し、仕様/計画/作業ログを `docs/` に集約。Electronの起動時HTML読み込みをcwd依存から `__dirname` 基準に修正。macOS向けにHomebrew前提のセットアップスクリプトを追加し、Node依存・Python venv導入・`.env` 作成を自動化。
- **変更ファイル**: `apps/web/*`, `apps/nfc_tool/*`, `docs/*`, `README.md`, `rules.md`
- **備考**: `.env.template` は環境側の制約で作成できなかったため、代替として `apps/nfc_tool/env.template` を採用し、`setup_mac.sh` はそれを `.env` にコピーする実装にしています。

## 15:10 - macOSでsmartcardモジュールが見つからない問題の対処
- **実施内容**: Electronが起動するPythonがvenvではなく環境側の `python` を参照してしまい `ModuleNotFoundError: No module named 'smartcard'` が出るケースに対応。`apps/nfc_tool/src/main.js` で `.venv/bin/python3` があればそれを優先して起動するようにし、venv導入済み環境で `pyscard`（smartcard）が確実に解決されるようにした。
- **変更ファイル**: `apps/nfc_tool/src/main.js`
- **備考**: `setup_mac.sh` 実行時に `pyscard` のビルドで失敗する場合は、`swig` やPC/SC周りの導入状況を確認する。

## 15:40 - デバッグ計測の撤去
- **実施内容**: Electron/Renderer/Pythonに追加していたデバッグ用NDJSONログ送信・debug.log追記の計測コードを撤去し、通常運用のコードに戻した。
- **変更ファイル**: `apps/nfc_tool/src/main.js`, `apps/nfc_tool/src/js/read.js`, `apps/nfc_tool/src/js/index.js`, `apps/nfc_tool/src/python/monitor_nfc.py`, `apps/nfc_tool/src/python/nfc_writer.py`
- **備考**: venvのPythonを優先して起動する修正（本体の修正）は維持。
